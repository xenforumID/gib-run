{
  "name": "server",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "test": "bun test src/tests/*.test.ts"
  },
  "dependencies": {
    "hono": "^4.11.4"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
